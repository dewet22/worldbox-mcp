{
  "_comment": "Add to ~/.config/opencode/config.json (Linux/macOS) or %APPDATA%\\opencode\\config.json (Windows).",
  "mcp": {
    "worldbox": {
      "type": "local",
      "command": ["uvx", "worldbox-mcp"],
      "enabled": true
    }
  }
}
